Place your hero image here:

This folder is served statically by Next.js. To display the banner at the top of the site, save the PNG file you provided as:

  public/BCYL_hero.png

Then the `HeroSection` component will automatically use it as the background.
